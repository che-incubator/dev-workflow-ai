schemaVersion: 2.2.2
metadata:
  name: dev-workflow-ai
  generateName: dev-workflow-ai-
  description: Autonomous AI software engineer for Eclipse Che — issue→PR→review→fix loop

components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi9-latest
      memoryLimit: 6G
      memoryRequest: 1024Mi
      cpuRequest: 1000m
      cpuLimit: 4000m
      mountSources: true
      endpoints:
        - exposure: public
          name: app
          protocol: https
          targetPort: 3000
          path: /
        - exposure: public
          name: webpack-dev
          protocol: https
          targetPort: 5173
          path: /
        - exposure: public
          name: swagger
          protocol: https
          targetPort: 3000
          path: /swagger
      env:
        - name: PGLITE_DATA_DIR
          value: "/projects/.data/pglite"
        - name: KNOWLEDGE_DIR
          value: "${PROJECT_SOURCE}/pg_seed/eclipse-che"
        - name: OLLAMA_MODEL
          value: "qwen2.5-coder:7b"

commands:
  - id: start-prepare
    exec:
      label: "[DWA] start:prepare — seed DB from pg_seed/"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn start:prepare"
      group:
        kind: run

  - id: dev
    exec:
      label: "[DWA] dev — start API (PGlite, hot-reload)"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn dev"
      group:
        kind: run
        isDefault: true

  - id: start
    exec:
      label: "[DWA] start — frontend dev server (port 5173)"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn start"
      group:
        kind: run

  - id: build
    exec:
      label: "[DWA] build — UI + API (production webpack)"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn build"
      group:
        kind: build
        isDefault: true

  - id: test
    exec:
      label: "[DWA] test"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn test"
      group:
        kind: test
        isDefault: true

  - id: run-issue
    exec:
      label: "[DWA] run-issue — run agent directly on an issue URL"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn run-issue"
      group:
        kind: run

  - id: license-generate
    exec:
      label: "[DWA] license:generate"
      component: tools
      workingDir: ${PROJECT_SOURCE}
      commandLine: "yarn license:generate"
      group:
        kind: build
