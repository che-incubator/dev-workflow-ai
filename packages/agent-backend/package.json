{
  "name": "@dev-workflow-ai/agent-backend",
  "version": "0.2.0",
  "description": "Autonomous AI engineer — Fastify API + LangGraph agent",
  "license": "EPL-2.0",
  "type": "module",
  "main": "lib/server/index.cjs",
  "scripts": {
    "build": "webpack --color --config webpack.config.cjs",
    "build:watch": "webpack --color --config webpack.config.cjs --watch",
    "format:check": "prettier --check 'src/**/*.ts'",
    "format:fix": "prettier --write 'src/**/*.ts'",
    "lint:check": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint 'src/**/*.ts' --fix",
    "test": "vitest run --project backend"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@langchain/core": "^1.2.10",
    "pino-pretty": "^13.1.3"
  }
}
